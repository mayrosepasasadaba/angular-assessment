export interface Contact {
    id: string|number,
    name: string,
    email: string,
    contact_no: string,
}

export interface NewContact {
    name: string,
    email: string,
    contact_no: string,
}